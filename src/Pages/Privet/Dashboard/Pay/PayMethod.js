import { Typography } from '@mui/material';
import React from 'react';

const PayMethod = () => {
    return (
        <Typography variant="h4">
            Pay Method Is Processing
        </Typography>
    );
};

export default PayMethod;